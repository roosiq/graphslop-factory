{
  "name": "@graphslop/edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy --keep-vars",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.115.0"
  }
}
